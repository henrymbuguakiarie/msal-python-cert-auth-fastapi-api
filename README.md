# msal-python-cert-auth-fastapi-api
Secure Python sample using MSAL with X.509 certificate authentication to sign in users and call a protected FastAPI microservice. Showcases JWT validation, delegated OAuth scopes, Poetry dependency management, and CRUD APIs.
